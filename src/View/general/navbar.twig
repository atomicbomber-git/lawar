{% extends 'general/navbar_base.twig' %}

{# Navigation bar to be used for pages available only to users that have been logged in #}
{% block menu %}
    <ul class="nav navbar-nav navbar-right">
        <li>
            <a class="menu" href="{{ path_for('home') }}">
                <span class="glyphicon glyphicon-home"></span>
                Home
            </a>
        </li>

        <li>
            <a class="menu" href="{{ path_for('inventory-item-search') }}">
                <span class="glyphicon glyphicon-search"></span>
                Cari
            </a>
        </li>




        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <span class="glyphicon glyphicon-cog"></span>
                    Kelola
                <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
                {% if user.privilege == "ADMINISTRATOR" or user.privilege == "MANAGER" %}
                    <li>
                        <a class="menu" href="{{ path_for('inventory') }}"> <span class="glyphicon glyphicon-list"></span> Daftar Item </a>
                    </li>

                    <li>
                        <a href="{{ path_for('cart') }}"> <span class="glyphicon glyphicon-shopping-cart"></span> Keranjang </a>
                    </li>

                    <li>
                        <a href="{{ path_for('cash_register') }}"> <span class="glyphicon glyphicon-usd"></span> Tarik / Setor Kas </a>
                    </li>

                    <li>
                        <a href="{{ path_for('inventory-item-add') }}"> <span class="glyphicon glyphicon-plus"></span> Tambah Item </a>
                    </li>

                    <li>
                        <a href="{{ path_for('type') }}"> <span class="glyphicon glyphicon-cog"></span> Tipe Item </a>
                    </li>

                    <li>
                        <a href="{{ path_for('ledger-input') }}"> <span class="glyphicon glyphicon-book"></span> Catatan Keuangan </a>
                    </li>

                    <li>
                        <a href="{{ path_for("invoice-transaction-list") }}"> <span class="glyphicon glyphicon-list-alt"></span> Catatan Transaksi </a>
                    </li>

                    {% if user.privilege == "ADMINISTRATOR" %}
                        <li>
                            <a href="{{ path_for("user-manage") }}"> <span class="glyphicon glyphicon-user"></span> Kelola Akun </a>
                        </li>
                    {% endif %}
                {% else %}
                    <li>
                        <a class="menu" href="{{ path_for('inventory') }}"> <span class="glyphicon glyphicon-list"></span> Daftar Item </a>
                    </li>
                    <li>
                        <a href="{{ path_for('cart') }}"> <span class="glyphicon glyphicon-shopping-cart"></span> Keranjang </a>
                    </li>
                {% endif %}
            </ul>
        </li>


        <li>
            <a class="menu" href="{{ path_for('logout') }}">
                <span class="glyphicon glyphicon-log-out"></span>
                Keluar
            </a>
        </li>

    </ul>
{% endblock %}
